# Copy to .env and fill in. .env is gitignored.

# SQLite (local dev). For production use a Postgres URL from Neon/Supabase
# and set provider = "postgresql" in prisma/schema.prisma.
DATABASE_URL="file:./dev.db"

# openssl rand -base64 32
AUTH_SECRET=""
AUTH_URL="http://localhost:3000"

# Owner WhatsApp number for manual registration (no '+', wa.me format)
NEXT_PUBLIC_OWNER_WHATSAPP="923415298183"
